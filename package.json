{
  "name": "typing-engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static typing game served from the public folder.",
  "scripts": {
    "dev": "node scripts/serve-public.js",
    "start": "node scripts/serve-public.js",
    "check": "node --check public/main.js && node --check public/texts.js && node --check scripts/serve-public.js"
  }
}
